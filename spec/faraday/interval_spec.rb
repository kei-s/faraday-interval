require 'spec_helper'

describe Faraday::Interval do
  it 'has a version number' do
    expect(Faraday::Interval::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
